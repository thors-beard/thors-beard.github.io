<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <style>
    :root {
      --bg: #ffffff;
      --bolt: #000000;
    }
    
    @media (prefers-color-scheme: dark) {
      :root {
        --bg: #000000;
        --bolt: #ffffff;
      }
    }
  </style>

  <!-- Bakgrund -->
  <circle cx="32" cy="32" r="30" fill="var(--bg)"/>

  <!-- Blixt -->
  <path d="M36 6 L16 34 H30 L24 58 L48 26 H34 L36 6 Z" fill="var(--bolt)"/>
</svg>
